name: carematch-rl
version: 1
entrypoint:
  type: http
  base_url: /
  routes:
    reset:
      method: POST
      path: /reset
    step:
      method: POST
      path: /step
healthcheck:
  method: GET
  path: /
